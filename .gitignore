node_modules/
lighthouse-reports/
.env
.env.local
# Local user export for one-off migration — never commit real customer data
users.json
.env.*.local
# Supabase CLI local link state (optional); migrations stay in repo
.supabase/
.cursor/
.cursor
# Vercel CLI: track project link only (see https://vercel.com/docs/projects/overview#project-json)
.vercel/*
!.vercel/project.json
